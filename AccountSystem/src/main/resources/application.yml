server:
  port: 8080
grpc:
  server:
    port: 9090
mybatis:
  mapper-locations: classpath:mappers/*xml
  configration:
    local-cache-scope: statement
pagehelper:
  reasonable: false
  defaultCount: true
spring:
  application:
    name: account-grpc-server
  profiles:
    include: @activatedProperties@